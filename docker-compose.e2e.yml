services:
  backend:
    build:
      context: ./backend
    environment:
      - DATABASE_URL=${DATABASE_URL}
    dns:
      - 8.8.8.8

  frontend:
    build:
      context: ./frontend
    depends_on:
      - backend